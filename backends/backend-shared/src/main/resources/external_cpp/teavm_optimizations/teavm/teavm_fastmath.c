#include "../pure/fastmath.h"

float teavm_fastmath_sin_deg(float degrees) {
    return fastmath_sin_deg(degrees);
}

float teavm_fastmath_cos_deg(float degrees) {
    return fastmath_cos_deg(degrees);
}
