package com.badlogic.gdx.physics.bullet.collision;

/**
 * @author xpenatan
 */
public class btOverlappingPairCache extends btOverlappingPairCallback {
    protected btOverlappingPairCache(String className) {
        super(className);
    }
}