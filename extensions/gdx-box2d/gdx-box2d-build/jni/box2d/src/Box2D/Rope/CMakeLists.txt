
INCLUDE_DIRECTORIES(
	${BOX2D_PHYSICS_SOURCE_DIR}/src
)

SET(Rope_SRCS
	b2Rope.cpp
)

SET(Rope_HDRS
	b2Rope.h
)

ADD_LIBRARY(Rope ${Rope_SRCS} ${Rope_HDRS})
SET_TARGET_PROPERTIES(Rope PROPERTIES VERSION ${BOX2D_VERSION})
#SET_TARGET_PROPERTIES(Rope PROPERTIES SOVERSION ${BOX2D__VERSION})